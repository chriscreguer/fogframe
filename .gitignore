# secrets — never commit
.env
dropbox.env
dropbox_refresh.json

# python
__pycache__/
*.pyc
venv/
.venv/

# build artifacts (frame.bin + preview are committed by CI to a separate path)
build/
*.DS_Store
